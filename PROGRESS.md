# PROGRESS.md — память проекта (читать при каждом новом заходе)

Обновляется после КАЖДОГО изменения. Этот файл — главный источник
контекста при продолжении работы с любого аккаунта Kimi.

## Текущее состояние (обновлено: 2026-09-25, v3.1 готова к деплою)

- **Версия кода: 3.1 — готова, ждёт загрузки на GitHub.**
- Что меняется в 3.1: банк больше не сбрасывается при построении сетки.
  Миграция старого state.json предусмотрена (ваши 42 USDT + 0.3447 SOL
  и start_equity 82.77 сохранятся).
- Работает на Railway, PAPER, SOLUSDT. Авто-resume (3.0) в проде и работает.
- Volume `/data` подтверждён — статистика переживает рестарты.
- Память проекта — в репозитории (AGENTS.md + этот файл).

## В работе (2026-09-25)

1. [x] v3.0: авто-resume — задеплоено и подтверждено в Telegram.
2. [x] v3.0: README/AGENTS/PROGRESS — в репозитории.
3. [x] v3.1: перенос банка между запусками (`paper_bot.py`), тесты пройдены
       (свежий старт / перезапуск / миграция старого состояния).
4. [ ] Загрузить на GitHub: `paper_bot.py`, `README.md`, `AGENTS.md`,
       `PROGRESS.md` → commit → деплой Railway.
5. [ ] После деплоя проверить в Telegram: Status — «start 82.77» должен
       остаться, эквити ~82.8-83, баланс 42 USDT + ~0.34 SOL (без сброса!).

## История изменений

### 2026-09-25 — v3.1: банк переживает запуски (backlog #2)
- `_init_bank(price, levels)`: первый запуск делит `START_BALANCE` (100)
  пропорционально уровням сетки между USDT и базовой монетой.
- `build_grid()`: балансы НЕ сбрасываются; `start_equity` фиксируется
  один раз (Itog PnL теперь всегда от начального депозита).
- Миграция: старые state.json без `bank_initialized` считаются
  проинициализированными — банк и статистика не теряются.
- Тесты (симуляция): свежий старт 50/50 USDT+SOL, эквити 100;
  перезапуск сохраняет баланс и start_equity; миграция вашего
  состояния (42/0.3447/82.77) — без потерь; цикл buy→sell корректен.
- Обновлены README.md и AGENTS.md.

### 2026-09-25 — v3.0: авто-resume + «память в GitHub»
- main.py: авто-resume (`state["active"]` → автозапуск после рестарта,
  уведомление через `TelegramController.send`).
- telegram_bot.py: метод `send()`.
- README.md, AGENTS.md, PROGRESS.md — добавлены.
- Деплой успешен: «Bot avtomaticheski zapushchen posle restarta» 05:22,
  Volume `/data` работает (6 циклов / 14 сделок сохранились).

### До 2026-09-25 — версия 2.2
- A1: продажи стартового инвентаря по себестоимости (inv_cost).
- A2: парные циклы по уровням, fallback — инвентарь.
- A3: slippage 0.05%. A4: watchdog. B1: дневной лимит просадки 5%.
- B2: ежедневный дайджест. Настройки кнопками, бэкап «Logi».

## Известные ограничения / замечания

1. При резком движении цены за один тик может исполниться несколько
   уровней; зеркальный ордер на соседний уровень перезаписывает
   существующий. Для paper-режима некритично. (Backlog #3)
2. Один снапшот цены на все исполнения тика — без внутритиковой динамики.
3. ~~Банк сбрасывался между запусками~~ — ИСПРАВЛЕНО в v3.1.

## Бэклог идей (по приоритету)

1. **График эквити**: раз в час писать equity в CSV и слать график
   в Telegram.
2. ~~Перенос банка между запусками~~ — СДЕЛАНО в v3.1.
3. **Защита от перезаписи зеркальных ордеров** при гэпах цены.
4. **Мультисимвол**: SYMBOL из настроек Telegram.
5. **Бэкап state в Telegram по расписанию**.
6. (Отложено) Реальный режим: ключи Bybit, тестнет.

## Как продолжать работу (для любого нового агента/сессии)

1. Прочитать `AGENTS.md` (правила) и этот файл (состояние).
2. Взять задачу из «Бэклог» (сейчас рекомендуется #1 — график эквити).
3. Реализовать, обновить версию в docstring, отметить галочками здесь.
4. Отдать владельцу файлы для Upload files на GitHub + commit.
5. Проверить в Telegram (Status) после деплоя.
