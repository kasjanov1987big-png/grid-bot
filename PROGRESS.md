# PROGRESS.md — память проекта (читать при каждом новом заходе)

Обновляется после КАЖДОГО изменения. Этот файл — главный источник
контекста при продолжении работы с любого аккаунта Kimi.

## Текущее состояние (обновлено: 2026-09-25)

- **Версия кода: 2.2 → готовится 3.0** (авто-resume, файлы документации).
- Проект работает на Railway в PAPER-режиме, пара по умолчанию SOLUSDT.
- Управление через Telegram-бота, всё работает, статистика копится.
- Ведение проекта перенесено на «память в репозитории»: AGENTS.md +
  этот файл — чтобы можно было продолжать работу с любого аккаунта Kimi.

## В работе (2026-09-25, заход с нового аккаунта)

1. [ ] `main.py` v3.0 — авто-resume после рестарта Railway (готов, ждёт пуша).
2. [ ] `telegram_bot.py` — добавить метод `send()` для уведомлений
       из `main.py` (патч, ждёт применения).
3. [ ] Добавить файлы: README.md, AGENTS.md, PROGRESS.md (этот файл).
4. [ ] Railway: убедиться, что Volume `/data` подключён и `DATA_DIR=/data`
       (иначе состояние и статистика сгорают при рестарте).

## История изменений

### 2026-09-25 — переход на «память в GitHub» + авто-resume
- Заведены README.md, AGENTS.md, PROGRESS.md.
- main.py: авто-resume (`state["active"]` → автозапуск после рестарта,
  уведомление админу через новый метод `TelegramController.send`).
- Telegram-бот: добавлен метод `send()`.
- Владелец работает с разных аккаунтов Kimi; контекст проекта хранится
  в репозитории (этот файл + AGENTS.md), а не в чатах.

### До 2026-09-25 — версия 2.2 (сделано ранее)
- A1: продажи стартового инвентаря по себестоимости (inv_cost).
- A2: парные циклы по уровням (buy idx → sell idx+1), fallback — инвентарь.
- A3: slippage при исполнении (по умолчанию 0.05%).
- A4: watchdog — уведомление при 5 ошибках подряд и при восстановлении.
- B1: дневной лимит просадки (5%) — остановка + уведомление.
- B2: ежедневный дайджест в Telegram.
- Управление настройками кнопками из Telegram, бэкап trades.csv/state.json
  одной кнопкой «Logi».

## Известные ограничения / замечания

1. `build_grid()` при каждом запуске сбрасывает балансы к фиксированному
   банку (`quote * кол-во уровней * 1.05`), а реализованный PnL при этом
   копится в stats — эквити и PnL в статусе могут расходиться между
   запусками. Решение: либо переносить банк между запусками, либо
   считать эквити отдельно от стартового банка. (Backlog #2)
2. При резком движении цены за один тик может исполниться несколько
   уровней; зеркальный ордер на соседний уровень перезаписывает
   существующий. Для paper-режима некритично. (Backlog #3)
3. Один снапшот цены на все исполнения тика — без внутритиковой динамики.

## Бэклог идей (paper-улучшения, по приоритету)

1. **График эквити**: раз в час писать equity в CSV и строить/слать
   график в Telegram (видно, как сетка работает во времени).
2. **Перенос банка между запусками**: не сбрасывать балансы в
   `build_grid`, а продолжать от текущих; отдельно вести «депозит».
3. **Защита от перезаписи зеркальных ордеров** при гэпах цены.
4. **Мультисимвол**: SYMBOL из настроек Telegram, несколько ботов-потоков.
5. **Бэкап state в Telegram по расписанию** (не только по кнопке).
6. (Отложено до paper-стабильности) Реальный режим: ключи Bybit,
   тестнет, реальные ордера.

## Как продолжать работу (для любого нового агента/сессии)

1. Прочитать `AGENTS.md` (правила) и этот файл (состояние).
2. Взять задачу из «В работе» или «Бэклог».
3. Реализовать, обновить версию в docstring, отметить галочками здесь.
4. Дать владельцу команды `git add / commit / push`.
5. Проверить в Telegram, что бот жив (Status) и деплой прошёл.
